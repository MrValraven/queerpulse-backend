meta {
  name: Get Messages
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/conversations/{{conversationId}}/messages?before=&limit=50
  body: none
  auth: none
}

docs {
  Active-only. Newest-first (createdAt DESC). before = ISO cursor (load older); limit <= 100. Returns MessageView[].
}
