meta {
  name: Accept Invite
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/invites/{{inviteCode}}/accept
  body: none
  auth: none
}

headers {
  Content-Type: application/json
  X-CSRF-Token: {{csrfToken}}
}

docs {
  Pending-ok. Promotes the redeemer. Returns { ok: true }. Call /auth/refresh afterwards to refresh the status claim.
}
