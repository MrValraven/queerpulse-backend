meta {
  name: Cancel Event
  type: http
  seq: 5
}

post {
  url: {{baseUrl}}/events/{{eventSlug}}/cancel
  body: none
  auth: none
}

headers {
  Content-Type: application/json
  X-CSRF-Token: {{csrfToken}}
}

docs {
  Active-only (host/co-host). Returns EventDetail with status 'cancelled'.
}
